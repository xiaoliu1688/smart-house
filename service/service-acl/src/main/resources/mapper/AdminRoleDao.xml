<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.atguigu.dao.AdminRoleDao">

    <!-- 用于select查询公用抽取的列 -->
    <sql id="columns">
        select id, role_id, admin_id, create_time, update_time, is_deleted
        from shf.acl_admin_role
    </sql>

<!--    通过用户id查找已经分配的角色-->
    <select id="findRoleByAdminId" resultType="long">
        select role_id
        from shf.acl_admin_role
        where admin_id = #{adminId}
    </select>

<!--    通过用户id删除所有角色-->

    <update id="deleteByAdminId">
        update shf.acl_admin_role
        set is_deleted = 1
        where admin_id = #{adminId}
    </update>

<!--    根据用户id和角色id添加角色-->
    <insert id="insert">
        insert into shf.acl_admin_role (role_id, admin_id)
        values (#{roleId},#{adminId})
    </insert>


</mapper>